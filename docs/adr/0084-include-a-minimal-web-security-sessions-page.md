# Include a minimal Web security sessions page

Because OAuth client consent is remembered until revoked, the Web entrypoint must include a minimal security sessions page that lists authorized CLI/MCP/Web sessions and allows revocation. This is a security surface, not a full operational dashboard.
