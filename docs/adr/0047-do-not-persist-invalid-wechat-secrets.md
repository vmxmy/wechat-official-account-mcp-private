# Do not persist invalid WeChat secrets

If WeChat credential validation fails, the submitted AppSecret and related credentials will not be persisted. Operators must resubmit credentials after fixing IP allowlisting or credential errors, reducing the retained surface of invalid or mistyped secrets.
