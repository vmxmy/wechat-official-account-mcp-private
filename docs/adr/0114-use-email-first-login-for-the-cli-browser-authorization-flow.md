# Use email-first login for the CLI browser authorization flow

`woa login` will open the same hosted authorization experience with email code login as the default path and GitHub as an optional provider. CLI registration remains `login`-driven, but the identity UX matches the Web entrypoint.
