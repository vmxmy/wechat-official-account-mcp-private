# Use Astryx as the required Web design system

The SaaS Web entrypoint will use Astryx as the required design system. New Web pages must use `@astryxdesign/core`, Astryx templates/components, `ThemeProvider`, `LinkProvider`, and the required CSS layer order rather than hand-rolled Tailwind UI.
