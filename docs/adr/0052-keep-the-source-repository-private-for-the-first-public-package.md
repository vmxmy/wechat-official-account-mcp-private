# Keep the source repository private for the first public package

The first public `@ziikoo/woa` npm package will be published from a private repository. The repository can be reconsidered for public release later after a dedicated history, secret, license, and support-surface audit.
