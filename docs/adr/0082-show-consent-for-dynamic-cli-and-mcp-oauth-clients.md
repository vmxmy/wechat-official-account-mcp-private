# Show consent for dynamic CLI and MCP OAuth clients

After Operator login, dynamic CLI and MCP OAuth clients must show a consent page with the client identity and requested scopes before authorization completes. This protects users from silently granting public dynamically registered clients access to their tenant.
