# Open and print Stripe Checkout URLs from the CLI

The CLI billing checkout command will request a Checkout session, print the URL, and attempt to open it in the user's browser. This supports both desktop and headless usage while keeping payment completion in Stripe-hosted checkout.
