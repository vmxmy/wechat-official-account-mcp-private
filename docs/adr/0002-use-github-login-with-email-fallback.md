# Use email code login first with GitHub as an optional provider

The first public SaaS onboarding flow will use six-digit email code login as the primary user-facing identity path and GitHub as an optional third-party login provider. This better matches WeChat Official Account operators who may not be developer-first, while still supporting GitHub identity for CLI/MCP-heavy users.
