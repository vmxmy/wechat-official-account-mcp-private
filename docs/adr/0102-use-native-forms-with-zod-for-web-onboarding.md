# Use native forms with Zod for Web onboarding

The Web onboarding forms will use native form submission patterns, Astryx form components, and Zod validation rather than introducing a form library. Shared validation schemas should be reused where practical so Web feedback and backend validation stay aligned.
