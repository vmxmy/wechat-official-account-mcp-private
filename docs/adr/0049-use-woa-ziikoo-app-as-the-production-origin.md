# Use woa.ziikoo.app as the production origin

The hosted SaaS production origin will be `https://woa.ziikoo.app`. OAuth callbacks, Stripe success/cancel URLs, MCP configuration, and email links should use this origin in the first release.
