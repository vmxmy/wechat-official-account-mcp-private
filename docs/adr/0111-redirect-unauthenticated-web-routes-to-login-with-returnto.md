# Redirect unauthenticated Web routes to login with returnTo

Protected Web routes will redirect unauthenticated Operators to the login page with a `returnTo` target. After login, the Web session should return the Operator to the originally requested onboarding, billing, MCP config, or security page.
