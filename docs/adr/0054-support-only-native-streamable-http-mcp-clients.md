# Support only native Streamable HTTP MCP clients

The first SaaS release will officially support MCP clients that can connect to the hosted Streamable HTTP `/mcp` endpoint with OAuth. The product will not restore a local MCP server and will not document `mcp-remote` as an official bridge path for the first release.
