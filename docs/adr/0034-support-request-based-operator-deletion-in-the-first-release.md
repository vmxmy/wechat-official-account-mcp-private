# Support request-based Operator deletion in the first release

The first release will support Operator deletion as a request-based flow rather than immediate full self-service deletion. The flow should cancel active subscriptions, purge WeChat secrets, disable access, and leave final data cleanup to an auditable support or background process.
