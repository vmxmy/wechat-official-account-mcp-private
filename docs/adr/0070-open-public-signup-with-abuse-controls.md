# Open public signup with abuse controls

The first SaaS release will allow public Free signup rather than requiring invitation codes or internal-only access. Abuse risk is handled through Turnstile, rate limits, quotas, OAuth identity controls, and audit logging.
