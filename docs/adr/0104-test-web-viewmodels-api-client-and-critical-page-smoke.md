# Test Web viewmodels, API client boundaries, and critical page smoke

The first Web testing layer will cover pure viewmodel/API-client behavior, route guard decisions, and smoke rendering for critical onboarding, billing, MCP config, and security session pages. Full browser E2E is deferred so the first release is not blocked by automating OAuth, Stripe, and WeChat production flows.
