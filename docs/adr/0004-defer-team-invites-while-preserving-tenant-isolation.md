# Defer team invites while preserving tenant isolation

The first SaaS release will enforce tenant isolation with a single Tenant owner rather than shipping full team invitations and role management. This keeps the security boundary and data model aligned with future multi-user tenants while reducing onboarding, authorization, and UI scope for the initial release.
