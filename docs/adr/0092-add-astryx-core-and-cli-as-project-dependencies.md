# Add Astryx core and CLI as project dependencies

The project will add `@astryxdesign/core` as a runtime dependency and `@astryxdesign/cli` as a development dependency for the Web entrypoint. This keeps Astryx component APIs, templates, and dense documentation available reproducibly in local development and CI.
