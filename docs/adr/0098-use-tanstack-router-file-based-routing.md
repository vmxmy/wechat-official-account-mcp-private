# Use TanStack Router file-based routing

The SaaS Web entrypoint will use TanStack Router file-based routing rather than a hand-maintained route tree. This makes page additions explicit in the route file structure and aligns with a scalable Web entrypoint as onboarding, billing, MCP config, and sessions pages are added.
