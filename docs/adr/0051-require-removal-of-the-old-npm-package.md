# Require removal of the old npm package

The old `@ziikoo/wechat-official-account-mcp` npm package should be removed rather than maintained or merely deprecated. If npm registry policy prevents self-service unpublish, the release path must treat removal as an external npm-support blocker instead of silently falling back to a deprecated package.
