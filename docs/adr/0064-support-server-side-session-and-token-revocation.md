# Support server-side session and token revocation

The SaaS will store OAuth/client sessions so Operators can review and revoke Web, CLI, and MCP authorizations from the server side. CLI-only local logout is not sufficient for the first public SaaS security model.
