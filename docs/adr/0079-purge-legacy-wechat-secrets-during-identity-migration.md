# Purge legacy WeChat secrets during identity migration

The new identity migration will clear legacy WeChat AppSecrets, webhook credentials, and access tokens from the default account/config storage. Existing Operators must reconfigure credentials through the new onboarding flow before WeChat operations resume.
