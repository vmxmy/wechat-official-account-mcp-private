# Meter total tool calls in addition to successful publishes

We will enforce a monthly tool call allowance across MCP, CLI, and API entrypoints in addition to the successful publish allowance. The first release limits are Free 300, Plus 3000, and Pro 30000 calls per tenant per month, which keeps the free plan usable while protecting WeChat API capacity and platform cost.
