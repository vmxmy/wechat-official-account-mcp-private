# Keep the public npm package under MIT

The public npm package will retain the MIT license. This matches the existing package metadata and keeps adoption friction low while the hosted SaaS remains the primary commercial product surface.
