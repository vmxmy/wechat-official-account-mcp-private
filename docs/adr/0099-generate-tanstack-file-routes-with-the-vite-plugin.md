# Generate TanStack file routes with the Vite plugin

TanStack Router file routes will be generated through the TanStack Router Vite plugin during development and build. The route tree should not rely on manual regeneration steps that can drift between local development, CI, and production deploys.
