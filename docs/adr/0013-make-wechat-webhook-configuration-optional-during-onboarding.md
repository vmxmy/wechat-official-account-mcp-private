# Make WeChat webhook configuration optional during onboarding

WeChat webhook Token/EncodingAESKey configuration will not be required to complete initial onboarding. Operators can activate outbound WeChat API operations first and configure the account-addressable callback URL and webhook credentials later when they need inbox or inbound-message features.
