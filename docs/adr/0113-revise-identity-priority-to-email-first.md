# Revise identity priority to email first

The identity priority is revised from GitHub-first to email-code-first, with GitHub retained as an optional third-party provider. The Web login UI, onboarding docs, and CLI browser login flow should present email verification as the default path.
