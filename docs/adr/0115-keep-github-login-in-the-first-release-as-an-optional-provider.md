# Keep GitHub login in the first release as an optional provider

GitHub login remains in the first public SaaS release, but it is presented as an optional provider after the email code path. This keeps developer-friendly identity support without making GitHub the default assumption for WeChat Official Account operators.
