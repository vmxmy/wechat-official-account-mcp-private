# Link Operator identities by verified email

GitHub identities and email fallback identities will resolve to the same Operator identity when they share a verified email address. Unverified or conflicting emails will not be auto-merged and must require explicit binding to avoid account takeover through unverified provider metadata.
