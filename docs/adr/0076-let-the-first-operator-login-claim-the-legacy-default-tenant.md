# Let the first Operator login claim the legacy default tenant

After the new identity system is deployed, the first successful Operator login will claim the legacy `tenant_default/acct_default` tenant and account. Public signup must not be considered open until that owner-claim step has completed.
