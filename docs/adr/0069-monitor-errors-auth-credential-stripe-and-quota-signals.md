# Monitor errors, auth, credential, Stripe, and quota signals

The first production monitoring scope will include Worker errors, OAuth/login failures, WeChat credential validation failures, Stripe webhook failures, and quota rejections. This gives enough operational visibility for onboarding support without requiring a full analytics dashboard first.
