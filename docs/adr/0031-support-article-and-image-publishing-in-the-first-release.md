# Support article and image publishing in the first release

The first SaaS release will explicitly support article publishing and image/贴图 publishing through the verified official-account publish flows already represented in the API contract. Video publishing will be shown as unsupported until a separate official API contract review and implementation proves a service-account-compatible path.
