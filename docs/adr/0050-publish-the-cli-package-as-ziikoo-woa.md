# Publish the CLI package as @ziikoo/woa

The public CLI package will use the short scoped npm name `@ziikoo/woa` while keeping the executable command as `woa`. This gives users a concise install command without relying on an unscoped package name.
