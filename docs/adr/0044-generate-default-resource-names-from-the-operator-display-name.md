# Generate default resource names from the Operator display name

The automatically bootstrapped tenant name will be generated from the Operator display name, while the first WeChat Official Account resource uses a clear default name that can be changed later. The domain language remains Tenant and WeChat Official Account resource rather than introducing workspace as a separate concept.
