# Accept direct D1 migration without backup

The SaaS onboarding migration will run directly against production D1 without a required pre-migration backup. This intentionally accepts the risk that schema or data migration mistakes may not be recoverable from a captured snapshot.
